# Production build only. Leave unset or empty to use the same origin as the page (recommended when
# the UI is served from FastAPI on :8001). Dev mode uses Vite's /api proxy instead.
# VITE_API_BASE=
